offregister_bootstrap
===============

## Install package

    pip install .

## Example use in offregister JSON

    {
        "module": "offregister-bootstrap",
        "kwargs": {
          "add_bootstrap": [
            "name": "ubuntu",
            "groups": null
          ]
        }
    }
